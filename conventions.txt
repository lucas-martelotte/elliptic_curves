CHUNK FILENAMES:
    a_b_c means (
        pygame y coord = a
        pygame x coord = b
        chunk size     = c
    )
CHUNK DICT:
    {group: [(a, b)]} means (
        pygame y coord (relative to chunk) = a
        pygame x coord (relative to chunk) = b
    )